interface:
  display_name: "Pokeori Deployment"
  short_description: "Release Pokeori through Coolify builds from main"
  default_prompt: "Use $deploy-pokeori to prepare and verify a Pokeori release automatically built from main by Coolify."
